/*
    document.getElementById('botonLogin').addEventListener('click', function () {
        var usuario = document.getElementById('usuario').value;
        var contrasena = document.getElementById('contrasena').value;

        if (usuario === '' && contrasena === '') {
        
            document.getElementById('iniciosesion').style.display = 'none';
            document.getElementById('menu').style.display = 'block';
            document.getElementById('retirar').style.display = 'none';
            document.getElementById('consignar').style.display = 'none';
            document.getElementById('transferir').style.display = 'none';
            document.getElementById('saldo').style.display = 'none';
            document.getElementById('registro').style.display = 'none';
        } else {
          
            alert('Credenciales Incorrectas');
        }
    });*/


    /*
var cerrarSesion = document.querySelector('.botonCerrar');
cerrarSesion.addEventListener('click', function() {
    //location.reload();}
    document.getElementById('iniciosesion').style.display = 'none';
    document.getElementById('menu').style.display = 'block';
    document.getElementById('retirar').style.display = 'none';
    document.getElementById('consignar').style.display = 'none';
    document.getElementById('transferir').style.display = 'none';
    document.getElementById('saldo').style.display = 'none';
    
});*/
// Add a variable to track the user's login state
//var isLoggedIn = false;

// Function to log the user out
//function logout() {
    /*isLoggedIn = false;
    // Add code to clear stored user data if you are using local storage or cookies.
    // You might also want to hide the main content and show the login form.
    document.getElementById('iniciosesion').style.display = 'block';
    document.getElementById('menu').style.display = 'none';
    // ... Other elements*/
//}

/*
// Modify your login function to update the login state
function login() {
    // Add code to verify user credentials here
    if () {
        isLoggedIn = true;
        document.getElementById('iniciosesion').style.display = 'none';
        document.getElementById('menu').style.display = 'block';
        // ... Show other elements
    } else {
        alert('Usuario no registrado o credenciales incorrectas');
    }
}*/

// Call the logout function when the page loads to ensure the initial state is correct
//logout();



/*
function consignarPlata() {
    var ingreso = document.getElementById('ingresos').value;
    var ingresos = parseFloat(ingreso);

    if (!isNaN(ingresos)) {
       
        var saldoElement = document.getElementById('saldos');
        var saldo = parseFloat(saldoElement.textContent);
        saldo += ingresos;
        saldoElement.textContent = saldo;

        
        document.getElement[DOM] Found 3 elements with non-unique id #: (More info: https://goo.gl/9p2vKq) <input type=​"password" placeholder=​"Contraseña" id>​ById('ingresos').value = "";
        console.log("Updated saldo: " + saldo);
        alert("Saldo actualizado")
    } else {
        alert("Invalido, Intente Otra vez");
    }
}*/
  /*  
function transferirDinero() {
    var nombre = document.querySelector('#transferir input[placeholder="Nombre de quien recibe."]').value;
    var documento = document.querySelector('#transferir input[placeholder="Documento O NIT."]').value;
    var tipoDeCuenta = document.querySelector('#transferir select[name="tipoDeCuenta"]').value;
    var numeroCuenta = document.getElementById('numero').value;
    var banco = document.querySelector('#transferir select[name="Bancos"]').value;
    var valorTransferencia = parseFloat(document.querySelector('#transferir input[placeholder="Valor a Transferir"]').value);

   

    var balanceElement = document.getElementById('saldos');
    var balance = parseFloat(balanceElement.textContent);

    if (valorTransferencia <= balance) {
       
        balance -= valorTransferencia;

       
        balanceElement.textContent = balance;

        var transferDetails = "Transfer Details:\n" +
            "Nombre: " + nombre + "\n" +
            "Documento o NIT: " + documento + "\n" +
            "Tipo de Cuenta: " + tipoDeCuenta + "\n" +
            "Número de Cuenta: " + numeroCuenta + "\n" +
            "Banco: " + banco + "\n" +
            "Valor a Transferir: " + valorTransferencia;

        alert(transferDetails);

       
        
   document.querySelector('#transferir input[placeholder="Nombre de quien recibe."]').value = "";
    document.querySelector('#transferir input[placeholder="Documento O NIT."]').value = "";
    document.querySelector('#transferir select[name="tipoDeCuenta"]').value = "1"; // Set default value
    document.getElementById('numero').value = "";
    document.querySelector('#transferir select[name="Bancos"]').value = "0"; // Set default value
    document.querySelector('#transferir input[placeholder="Valor a Transferir"]').value = "";
    } else {
        alert("No tiene suficiente saldo para realizar la transferencia.");
    }
}*/

/*
// At the beginning of your JavaScript file, before any functions
var users = [];

// Load user data from localStorage, if available
var storedUsers = localStorage.getItem('users');
if (storedUsers) {
    users = JSON.parse(storedUsers);
}

// Function to update user data and save to localStorage
function updateUserLocalStorage() {
    localStorage.setItem('users', JSON.stringify(users));
}

// Inside your envioRegistro() function after successfully registering a user
// (Add this code before the "User registered successfully" alert)
updateUserLocalStorage();*/

/*
function transferirDinero() {
    var nombre = document.querySelector('#transferir input[placeholder="Nombre de quien recibe."]').value;
    var documento = document.querySelector('#transferir input[placeholder="Documento O NIT."]').value;
    var tipoDeCuenta = document.querySelector('#transferir select[name="tipoDeCuenta"]').value;
    var numeroCuenta = document.getElementById('numero').value;
    var banco = document.querySelector('#transferir select[name="Bancos"]').value;
    var valorTransferencia = parseFloat(document.querySelector('#transferir input[placeholder="Valor a Transferir"]').value);

    var sender = null; // Store the sender user
    var recipient = null; // Store the recipient user
    
    // Find the sender and recipient users in the users array
    for (var i = 0; i < users.length; i++) {
        if (users[i].documentNumber === documento) {
            recipient = users[i];
        }
        if (users[i].documentNumber === senderDocumentNumber) {
            sender = users[i];
        }
    }

    
    if (recipient) {
        if (sender && valorTransferencia <= sender.balance) {
            sender.balance -= valorTransferencia;
            recipient.balance += valorTransferencia;

          
            balanceElement.textContent = sender.balance;

            var transferDetails = "Transfer Details:\n" +
                "Nombre: " + nombre + "\n" +
                "Documento o NIT: " + documento + "\n" +
                "Tipo de Cuenta: " + tipoDeCuenta + "\n" +
                "Número de Cuenta: " + numeroCuenta + "\n" +
                "Banco: " + banco + "\n" +
                "Valor a Transferir: " + valorTransferencia;

            alert(transferDetails);

           
            document.querySelector('#transferir input[placeholder="Nombre de quien recibe."]').value = "";
            document.querySelector('#transferir input[placeholder="Documento O NIT."]').value = "";
            document.querySelector('#transferir select[name="tipoDeCuenta"]').value = "1"; // Set default value
            document.getElementById('numero').value = "";
            document.querySelector('#transferir select[name="Bancos"]').value = "0"; // Set default value
            document.querySelector('#transferir input[placeholder="Valor a Transferir"]').value = "";
        } else {
            alert("No tiene suficiente saldo para realizar la transferencia.");
        }
    } else {
        alert("El documento del destinatario no se encontró en nuestros registros.");
    }
}
*/
/*
    var sender = null; // Store the sender user
    var recipient = null; // Store the recipient user
    
    // Find the sender and recipient users in the users array
    for (var i = 0; i < users.length; i++) {
        if (users[i].documentNumber === documento) {
            recipient = users[i];
        }
        if (users[i].documentNumber === senderDocumentNumber) {
            sender = users[i];
        }
    }*/